post standup work
post standup work
Test: commit with NO ticket id — should stay UNMATCHED. If a commit has no task/ticket key (like JRVIS20-10) in its message or branch, the matcher must NOT link it to any ticket.
Test JRVIS20-12: commit with ticket key should link to JRVIS20-12
JRVIS20-17: updated jira.py to read all workspaces. Notes: jira.py now fetches tickets across all boards/workspaces, not just one, so the matcher and standup see every sprint ticket.
